package config

const (
	// AppName is the human-readable application name.
	AppName    = "Developer Toolbox"
	AppVersion = "0.5.0"
	// DefaultTheme is the default UI theme.
	DefaultTheme = "dark"
	// DefaultLanguage is the default UI language.
	DefaultLanguage = "en"
	// DefaultJSONIndent is the default indentation width used by JSON operations.
	DefaultJSONIndent = 2
	// StorageDir is the base directory for local JSON data storage.
	StorageDir = "data"
)
